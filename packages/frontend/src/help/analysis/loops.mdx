import { A } from "@solidjs/router";

#### User inputs

<dl>
  <dt>Limit length of paths: true / false</dt>
  <dd>Whether or not to limit the number of paths composed in order to find loops.</dd>
  <dt>Maximum length of path: $\mathbb{Z}_{\geqslant0}$</dt>
  <dd>A non-negative integer determining the maximum number of paths to compose in order to find loops.</dd>
</dl>
