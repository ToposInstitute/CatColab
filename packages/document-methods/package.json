{
    "name": "catcolab-document-methods",
    "version": "0.1.0",
    "private": true,
    "license": "MIT",
    "type": "module",
    "exports": {
        ".": "./src/index.ts",
        "./*": "./src/*"
    },
    "scripts": {
        "build:wasm": "wasm-pack build ../document-types",
        "format": "oxfmt .",
        "lint": "oxlint --fix && oxfmt .",
        "test:lts": "cd test/literate-ts && literate-ts --nocache ../*.lts.md",
        "check": "pnpm run build:wasm && tsc && pnpm run lint",
        "ci": "tsc && oxlint --deny-warnings && oxfmt --check ."
    },
    "dependencies": {
        "catcolab-document-types": "link:../document-types/pkg",
        "tiny-invariant": "^1.3.3",
        "uuid": "^13.0.0"
    },
    "devDependencies": {
        "@types/uuid": "^10.0.0",
        "literate-ts": "^2.5.0",
        "typescript": "^6.0.3",
        "wasm-pack": "^0.15.0"
    }
}
